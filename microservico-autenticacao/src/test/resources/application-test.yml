spring:
  datasource:
    url: jdbc:h2:mem:testauthdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never

jwt:
  secret: test-jwt-secret-key-com-minimo-32-chars
  expiration-ms: 3600000
